#include <cstdlib>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(){
    int n, k;
    cin >> n >> k;
    vector<int> d(n);
    for (size_t i = 0; i < n; i++)
    {
        cin >> d[i];
    }
    string n_str = to_string(n);
    for (size_t i = 0; i < n_str.size(); i++)
    {
        
    }
    


    return 0;
}
